export const getId = (state, params) => params.id;

export const getUserId = (state, params) => params.userId;

export const getMajorId = (state, params) => params.majorId;

export const getQuestionId = (state, params) => params.questionId;

export const getVideoLinkId = (state, params) => params.videoLinkId;

export const getCreditId = (state, params) => params.creditId;
